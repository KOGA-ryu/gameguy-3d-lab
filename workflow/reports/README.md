# Workflow Reports

Completed worker and reviewer packets belong here.

Use one folder per packet:

```text
goal/workflow/reports/<packet-id>/
  report.md
  receipt.json
```

Do not scatter reports across engine source folders.

