# Workflow Inbox

Drop future Dex work orders here when they should be picked up by a worker or reviewer.

Use one Markdown file per work order.

Recommended packet name:

```text
<ROLE>-<NUMBER>-<short-task>.md
```

Example:

```text
3D-0008-polish-connector-kit-source-lane.md
```

